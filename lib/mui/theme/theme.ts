import { createTheme, colors } from "@mui/material";

const theme = createTheme({
  status: {
    danger: '#e53e3e',
  },
  palette: {
    ali: {
      main: '#000',
    },
    white: {
      main: '#FFFFFF',
    },
  },
});

export default theme;
