import { createTheme } from "@mui/material";

const theme = createTheme({
  status: {
    danger: '#e53e3e',
  },
  palette: {
    ali: {
      main: '#000',
    },
    white: {
      main: '#FFFFFF',
    },
    dark: {
      main: '#806F6F',
    },
    purple: {
      main:"#07020d"
    }
  },
});

export default theme;
